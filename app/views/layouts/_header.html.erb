<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-offset-3 col-md-6 col-sm-12 text-center">
        <% if user_signed_in? %>
        <%= link_to 'Do it!', lists_path, class: 'navbar-brand navbar-btn' %>
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div id="navbar" class="navbar-collapse collapse" aria-expanded="false" style="height: 1px;">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to 'Create a new list', new_list_path, class: 'navbar-btn' %></li>
            <li><%= link_to "Logged as #{current_user.email}", lists_path, class: 'navbar-btn', readonly: true %></li>
            <li><%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "navbar-btn"  %></li>
          </ul>
        </div>
        <% else %>
        <%= link_to 'Do it!', root_path, class: 'navbar-brand navbar-btn' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
